"""
App config for the app
"""

from django.apps import AppConfig


class AppMainConfig(AppConfig):
    """
    App config for the app
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "app"
